@model IEnumerable<Topluluk_Duyuru.Models.Duyuru>

@{
    ViewBag.Title = "DUYURULAR";
}

<h2>DUYURULAR</h2>

<p>
    <b> @Html.ActionLink("Create New", "Create")</b>
</p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.duyuruKonu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.duyuruIcerik)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.duyuruSahip)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.duyuruBaslangic)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.duyuruBitis)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.duyuruKonu)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.duyuruIcerik)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.duyuruSahip)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.duyuruBaslangic)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.duyuruBitis)
                </td>
                <td>
                    <b>
                        @Html.ActionLink("Edit....", "Edit", new { id = item._id }, new { Style = "color:Green" }) |
                        @Html.ActionLink("Details", "Details", new { id = item._id }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item._id }, new { Style = "color:Red" })
                    </b>
                   
                </td>
            </tr>
        }

    </table>
